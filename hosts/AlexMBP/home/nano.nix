{
  home.file.".nanorc" = {
    text = ''
      # Editor:
      set morespace
      set tabstospaces
      set const

      # UX:
      set smooth

      # Extra files:
      set backup
      set backupdir "~/.nano-backups"
      set historylog

      # line numbers are copied with text,
      # it is not useful:
      # set linenumbers
    '';
  };
}
