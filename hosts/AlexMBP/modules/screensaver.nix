{ ... }:

{
  system.defaults.screensaver = {
    # Require the login password to dismiss the screen saver
    askForPassword = true;

    # Seconds after the screen saver starts before the password prompt appears
    askForPasswordDelay = 10;
  };
}
